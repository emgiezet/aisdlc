{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "slop-guard",
  "version": "0.1.0",
  "description": "Prevents, detects and gates security, performance and maintainability anti-patterns in agent-written code: per-language prevention skills, deterministic checks after each edit, and a Stop gate that blocks on unresolved blockers",
  "author": {
    "name": "Maksymilian Małecki"
  },
  "license": "MIT",
  "keywords": ["security", "sast", "linting", "anti-patterns", "supply-chain"],
  "extensions": {
    "com.openai": {
      "hooks": "./hooks/hooks.json",
      "interface": {
        "displayName": "Slop Guard",
        "shortDescription": "PreToolUse security gate for agent-written code",
        "longDescription": "Enforces linter-suppression justifications, blocks supply-chain installs, denies credential file access, detects hard-coded secrets, and gates quality-config lowering — at the PreToolUse boundary, across Claude Code, Codex, and Grok.",
        "developerName": "Maksymilian Małecki",
        "category": "Security",
        "capabilities": ["PreToolUse"]
      },
      "userConfig": {
        "enforcement_mode": {
          "type": "string",
          "title": "Enforcement mode",
          "description": "advisory | balanced | strict",
          "default": "balanced"
        },
        "stop_gate": {
          "type": "boolean",
          "title": "Block turn end on unresolved blockers",
          "description": "Run slow checks at Stop and keep Claude working while blockers remain.",
          "default": true
        },
        "sast_engine": {
          "type": "string",
          "title": "SAST engine",
          "description": "opengrep | semgrep | none",
          "default": "opengrep"
        },
        "allow_network": {
          "type": "boolean",
          "title": "Allow network in Stop gate",
          "description": "Enables dependency vulnerability lookups (SCA).",
          "default": false
        },
        "tool_source": {
          "type": "string",
          "title": "Tool source",
          "description": "project-first | plugin-only | project-only",
          "default": "project-first"
        },
        "config_source": {
          "type": "string",
          "title": "Config source mode",
          "description": "overlay | full | project-only",
          "default": "overlay"
        },
        "require_docs_lookup": {
          "type": "boolean",
          "title": "Require Context7 documentation lookup",
          "description": "Warn when a framework or new dependency is used without a Context7 lookup in the same session.",
          "default": true
        },
        "dependency_freshness": {
          "type": "string",
          "title": "Dependency freshness enforcement",
          "description": "off | warn | error — how to surface too-fresh or outdated pinned dependencies.",
          "default": "warn"
        },
        "dependency_cooldown_days": {
          "type": "string",
          "title": "Dependency cooldown window (days)",
          "description": "Versions published within this many days are flagged as too-fresh (supply-chain risk).",
          "default": "3"
        }
      }
    }
  }
}
