{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "slop-guard",
  "version": "0.1.0",
  "description": "PreToolUse policy enforcement for agent-written code: credential access guards, supply-chain install blocks, linter-suppression gates, and hard-coded secret detection.",
  "author": {
    "name": "Maksymilian Małecki"
  },
  "license": "MIT",
  "keywords": ["security", "linting", "anti-patterns", "supply-chain"],
  "extensions": {
    "com.openai": {
      "hooks": "./hooks/hooks.json",
      "interface": {
        "displayName": "Slop Guard",
        "shortDescription": "PreToolUse security gate for agent-written code",
        "longDescription": "Enforces linter-suppression justifications, blocks supply-chain installs, denies credential file access, detects hard-coded secrets, and gates quality-config lowering — at the PreToolUse boundary, across Claude Code, Codex, and Grok.",
        "developerName": "Maksymilian Małecki",
        "category": "Security",
        "capabilities": ["PreToolUse"]
      },
      "userConfig": {
        "enforcement_mode": {
          "type": "string",
          "title": "Enforcement mode",
          "description": "advisory | balanced | strict",
          "default": "balanced"
        },
        "tool_source": {
          "type": "string",
          "title": "Tool source",
          "description": "project-first | plugin-only | project-only",
          "default": "project-first"
        }
      }
    }
  }
}
