{
  "$comment": [
    "Merge the 'permissions' and 'enabledPlugins' blocks below into your project's",
    ".claude/settings.json (or your organisation's managed settings).",
    "",
    "Architecture note — two layers, one purpose:",
    "  Floor  : the 'permissions' block below.  Enforced by the Claude Code platform",
    "           before any hook runs.  Cannot be bypassed by the agent.",
    "  Defence: the pre-read and pre-write hooks.  Catch edge cases the permission",
    "           patterns do not cover and add richer diagnostic reasons.",
    "",
    "In a managed-settings deployment the platform layer is the hard guarantee;",
    "the hook is defence in depth.  In a project-only deployment the hook is the",
    "only enforcement layer, so the permissions block below MUST be present.",
    "",
    "Codex / Grok: neither host documents an equivalent project-level permissions",
    "file at the time of writing.  The pre-write hook enforces the guard-config",
    "protection for those hosts via hook_secret_deny, which is the only available",
    "enforcement layer on those runtimes."
  ],
  "permissions": {
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(./**/*.pem)",
      "Read(./**/*.key)",
      "Read(./secrets/**)",
      "Bash(npm install -g *)",
      "Bash(sudo *)",
      "Read(./.claude/settings.json)",
      "Read(./.claude/settings.local.json)",
      "Write(./.claude/settings.json)",
      "Write(./.claude/settings.local.json)",
      "Read(./.agents/**)",
      "Write(./.agents/**)",
      "Read(./.grok-plugin/**)",
      "Write(./.grok-plugin/**)",
      "Write(./plugins/*/hooks/hooks.json)",
      "Write(./plugins/*/.claude-plugin/plugin.json)",
      "Write(./plugins/*/plugin.json)"
    ],
    "ask": [
      "Bash(npm install *)",
      "Bash(pnpm add *)",
      "Bash(composer require *)",
      "Bash(go get *)",
      "Bash(pip install *)",
      "Bash(terraform apply *)",
      "Bash(kubectl apply *)"
    ]
  },
  "enabledPlugins": {
    "slop-guard@<marketplace>": true
  }
}
