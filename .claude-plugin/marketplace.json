{
  "name": "aisdlc",
  "owner": {
    "name": "aisdlc"
  },
  "metadata": {
    "description": "AI SDLC harness — the pipeline, the guardrails, and the evals that let cheap models deliver reviewable code",
    "version": "0.1.0"
  },
  "plugins": [
    {
      "name": "sdlc",
      "source": "./plugins/sdlc",
      "description": "Spec-driven delivery for agents: /sdlc:init generates the instruction hierarchy for your repo, then spec → mockup → implement → qa → ship runs unattended and lands a draft PR",
      "version": "0.1.0",
      "strict": true
    },
    {
      "name": "slop-guard",
      "source": "./plugins/slop-guard",
      "description": "PreToolUse policy enforcement for agent-written code: credential access guards, supply-chain install blocks, linter-suppression gates, and hard-coded secret detection — hooks fire at the tool boundary",
      "version": "0.1.0",
      "strict": true
    }
  ]
}
